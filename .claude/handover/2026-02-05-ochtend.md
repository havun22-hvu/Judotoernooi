# Handover 5 februari 2026 - Ochtend

## Gedaan
- Live verversing verplaatst van toernooi naar organisator niveau (nieuw Instellingen menu)
- Clubs unique constraint gefixed (per organisator i.p.v. globaal)
- Auto-create organisator's eigen club feature
- Waarschuwing voor ongewogen judoka's in noodplan schema's
- Route cache vernieuwd op production

## OPENSTAAND: Club Delete Werkt Niet

**Probleem:** Delete knop voor clubs in `/organisator/clubs` werkt niet. Geen console errors, niets gebeurt.

**Locatie:**
- View: `resources/views/organisator/clubs/index.blade.php` (regel 117-127)
- Controller: `ClubController::destroyOrganisator()` (regel 134+)
- Route: `DELETE {organisator}/clubs/{club}` → `organisator.clubs.destroy`

**Wat al gecheckt:**
- Route bestaat en is correct
- JavaScript `confirmDelete()` functie ziet er goed uit
- `withCount('judokas')` is aanwezig in controller
- Geen Laravel log errors

**Volgende stappen:**
1. Test lokaal of delete werkt
2. Voeg console.log toe aan `confirmDelete()` om te zien of JS wordt aangeroepen
3. Check of form submit daadwerkelijk gebeurt (network tab)
4. Mogelijk probleem met `addslashes()` bij speciale karakters in clubnaam

**Code snippet delete form:**
```blade
<form action="{{ route('organisator.clubs.destroy', [$organisator, $club]) }}" method="POST"
      onsubmit="return confirmDelete({{ $club->judokas_count }}, '{{ addslashes($club->naam) }}')">
    @csrf
    @method('DELETE')
    <button type="submit">Delete</button>
</form>
```

## KRITIEKE REGEL (nieuw in memory)

**NOOIT handmatig data fixen!** Als code niet werkt → fix de code, niet de database.
Dit staat nu in `C:\Users\henkv\.claude\projects\d--GitHub-JudoToernooi\memory\MEMORY.md`

## Context

- **Henk** = eigenaar/sitebeheerder (henkvu@gmail.com)
- **Havun** = bedrijfsnaam (havun22@gmail.com)
- Organisatoren (zoals Cees Veen) zijn klanten
